services:
  factmatrix-app:
    build: .
    image: factmatrix-ci-factmatrix-app
    ports:
      - "8501:8501"
    networks:
      - factmatrix-net

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring:/etc/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
    networks:
      - factmatrix-net

  node-exporter:
    image: prom/node-exporter:latest
    ports:
      - "9100:9100"
    networks:
      - factmatrix-net

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
    networks:
      - factmatrix-net

networks:
  factmatrix-net:
    driver: bridge
